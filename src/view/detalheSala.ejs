<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Detalhes da Sala - <%= sala.nome %></title>
  <link href="https://fonts.googleapis.com/css?family=Poppins:400,600,700&display=swap" rel="stylesheet" />
  <style>
    body {
      font-family: 'Poppins', sans-serif;
      background: linear-gradient(135deg, #232526 0%, #414345 100%);
      margin: 0;
      padding: 40px 10px;
      min-height: 100vh;
      display: flex;
      justify-content: center;
      align-items: center;
    }
    .container {
      background: rgba(255, 255, 255, 0.97);
      border-radius: 18px;
      padding: 32px 24px;
      max-width: 420px;
      width: 100%;
      box-shadow: 0 4px 24px 0 #0002;
      text-align: center;
    }
    .sala-img {
      width: 100%;
      height: 180px;
      object-fit: cover;
      border-radius: 18px;
      margin-bottom: 24px;
      background: #eee;
    }
    h1 {
      margin-bottom: 20px;
      color: #FFD700;
      font-size: 2rem;
      font-weight: 700;
      letter-spacing: 1px;
      text-shadow: 1px 2px 8px #0008;
    }
    p {
      font-size: 1.1rem;
      margin: 8px 0;
      color: #444;
    }
    ul {
      list-style: none;
      padding: 0;
      margin: 8px 0 24px 0;
      color: #444;
      text-align: left;
    }
    ul li {
      margin-bottom: 6px;
      padding-left: 12px;
      position: relative;
    }
    ul li:before {
      content: '•';
      color: #FFD700;
      position: absolute;
      left: 0;
      font-size: 1.2em;
      line-height: 1;
    }
    .status {
      font-weight: bold;
      color: #fff;
      padding: 8px 24px;
      border-radius: 20px;
      display: inline-block;
      font-size: 1rem;
      letter-spacing: 1px;
      box-shadow: 0 2px 8px #0001;
      margin-bottom: 24px;
    }
    .livre {
      background: linear-gradient(90deg, #43e97b 0%, #38f9d7 100%);
      color: #232526;
      text-shadow: 0 1px 2px #fff8;
    }
    .ocupado {
      background: linear-gradient(90deg, #f85032 0%, #e73827 100%);
      text-shadow: 0 1px 2px #0008;
    }
    form button {
      width: 100%;
      padding: 14px;
      background: linear-gradient(90deg, #FFD700 0%, #FFB300 100%);
      border: none;
      border-radius: 30px;
      color: #232526;
      font-size: 1.1rem;
      font-weight: 600;
      cursor: pointer;
      box-shadow: 0 2px 8px #0002;
      transition: background 0.3s, transform 0.2s;
      outline: 2px solid transparent;
    }
    form button:hover, form button:focus {
      background: linear-gradient(90deg, #FFB300 0%, #FFD700 100%);
      transform: translateY(-2px) scale(1.03);
      outline: 2px solid #FFD700;
    }
    form button:disabled {
      background: linear-gradient(90deg, #f85032 0%, #e73827 100%);
      color: #fff;
      cursor: not-allowed;
      text-shadow: 0 1px 2px #0008;
      font-weight: bold;
    }
    a {
      display: block;
      margin-top: 28px;
      color: #FFD700;
      text-decoration: none;
      font-size: 1rem;
      font-weight: 500;
      letter-spacing: 1px;
      transition: color 0.2s, text-decoration 0.2s;
    }
    a:hover {
      color: #FFB300;
      text-decoration: underline;
    }
    @media (max-width: 480px) {
      .container {
        padding: 24px 16px;
        max-width: 95vw;
      }
      h1 {
        font-size: 1.5rem;
      }
    }
  </style>
</head>
<body>
  <div class="container">
    <% if (sala.imagem) { %>
      <img src="<%= sala.imagem %>" alt="Foto da sala <%= sala.nome %>" class="sala-img" />
    <% } else { %>
      <img src="https://via.placeholder.com/420x180?text=Sem+Imagem" alt="Sem imagem para <%= sala.nome %>" class="sala-img" />
    <% } %>
    <h1><%= sala.nome %></h1>
    <p><strong>Capacidade:</strong> <%= sala.capacidade %> pessoas</p>
    <p><strong>Recursos:</strong></p>
    <ul>
      <% sala.recursos.forEach(function(recurso) { %>
        <li><%= recurso %></li>
      <% }) %>
    </ul>
    <div class="status <%= sala.status === 'livre' ? 'livre' : 'ocupado' %>">
      <%= sala.status.charAt(0).toUpperCase() + sala.status.slice(1) %>
    </div>

    <% if (sala.status === 'livre') { %>
      <form action="/salas/<%= sala._id %>/reservar" method="POST">
        <button type="submit">Reservar Sala</button>
      </form>
    <% } else { %>
      <button disabled style="background: linear-gradient(90deg, #f85032 0%, #e73827 100%); color: #fff; font-weight: bold; cursor: not-allowed;">Sala Ocupada</button>
    <% } %>

    <a href="/salas">← Voltar para lista de salas</a>
  </div>
</body>
</html>

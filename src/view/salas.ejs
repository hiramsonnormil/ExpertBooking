<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Salas Disponíveis</title>
  <link href="https://fonts.googleapis.com/css?family=Poppins:400,600,700&display=swap" rel="stylesheet">
  <style>
    body {
      font-family: 'Poppins', sans-serif;
      background: linear-gradient(135deg, #232526 0%, #414345 100%);
      margin: 0;
      padding: 40px 10px;
      min-height: 100vh;
      display: flex;
      flex-direction: column;
      align-items: center;
    }
    .header {
      width: 100%;
      max-width: 1200px;
      display: flex;
      justify-content: space-between;
      align-items: center;
      margin-bottom: 40px;
    }
    .header h1 {
      color: #FFD700;
      font-size: 2.5rem;
      font-weight: 700;
      letter-spacing: 2px;
      margin: 0;
      text-shadow: 1px 2px 8px #0008;
    }
    .add-btn {
      background: linear-gradient(90deg, #FFD700 0%, #FFB300 100%);
      color: #232526;
      font-weight: 600;
      border: none;
      border-radius: 30px;
      padding: 12px 28px;
      font-size: 1.1rem;
      cursor: pointer;
      box-shadow: 0 2px 8px #0002;
      transition: background 0.3s, transform 0.2s;
      outline: 2px solid transparent;
    }
    .add-btn:focus, .add-btn:hover {
      background: linear-gradient(90deg, #FFB300 0%, #FFD700 100%);
      transform: translateY(-2px) scale(1.03);
      outline: 2px solid #FFD700;
    }
    .salas-container {
      display: flex;
      flex-wrap: wrap;
      gap: 32px;
      justify-content: center;
      width: 100%;
      max-width: 1200px;
    }
    .sala-card {
      display: flex;
      flex-direction: column;
      background: rgba(255,255,255,0.95);
      border-radius: 18px;
      box-shadow: 0 4px 24px 0 #0002;
      width: 340px;
      min-height: 420px;
      text-decoration: none;
      color: #232526;
      transition: box-shadow 0.3s, transform 0.2s;
      overflow: hidden;
      outline: 2px solid transparent;
    }
    .sala-card:focus, .sala-card:hover {
      box-shadow: 0 8px 32px 0 #FFD70055;
      transform: translateY(-4px) scale(1.02);
      outline: 2px solid #FFD700;
    }
    .sala-img {
      width: 100%;
      height: 180px;
      object-fit: cover;
      background: #eee;
      border-top-left-radius: 18px;
      border-top-right-radius: 18px;
    }
    .sala-info {
      padding: 24px 22px 18px 22px;
      display: flex;
      flex-direction: column;
      flex: 1;
    }
    .sala-info h3 {
      font-size: 1.4rem;
      font-weight: 700;
      margin: 0 0 10px 0;
      color: #232526;
      letter-spacing: 1px;
    }
    .sala-info p {
      margin: 4px 0;
      font-size: 1.05rem;
      color: #444;
    }
    .status {
      margin-top: 18px;
      font-weight: bold;
      color: #fff;
      padding: 7px 18px;
      border-radius: 20px;
      display: inline-block;
      font-size: 1rem;
      letter-spacing: 1px;
      box-shadow: 0 2px 8px #0001;
    }
    .livre {
      background: linear-gradient(90deg, #43e97b 0%, #38f9d7 100%);
      color: #232526;
      text-shadow: 0 1px 2px #fff8;
    }
    .ocupado {
      background: linear-gradient(90deg, #f85032 0%, #e73827 100%);
      text-shadow: 0 1px 2px #0008;
    }
    @media (max-width: 800px) {
      .salas-container {
        flex-direction: column;
        align-items: center;
      }
      .header {
        flex-direction: column;
        gap: 18px;
      }
    }
  </style>
</head>
<body>
  <div class="header">
    <h1>Salas Disponíveis</h1>
  </div>
  <div class="salas-container">
    <% if (salas.length === 0) { %>
      <p style="color:#fff;font-size:1.2rem;">Nenhuma sala cadastrada.</p>
    <% } else { %>
      <% salas.forEach(sala => { %>
        <a href="/salas/<%= sala._id %>" class="sala-card" tabindex="0" aria-label="Ver detalhes da sala <%= sala.nome %>">
          <% if (sala.imagem) { %>
            <img src="<%= sala.imagem %>" alt="Foto da sala <%= sala.nome %>" class="sala-img" />
          <% } else { %>
            <img src="https://via.placeholder.com/340x180?text=Sem+Imagem" alt="Sem imagem para <%= sala.nome %>" class="sala-img" />
          <% } %>
          <div class="sala-info">
            <h3><%= sala.nome %></h3>
            <p><strong>Capacidade:</strong> <%= sala.capacidade %></p>
            <p><strong>Recursos:</strong> <%= sala.recursos.join(', ') %></p>
            <span class="status <%= sala.status %>">
              <%= sala.status.charAt(0).toUpperCase() + sala.status.slice(1) %>
            </span>
          </div>
        </a>
      <% }) %>
    <% } %>
  </div>
</body>
</html>
